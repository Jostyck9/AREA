const AreaModel = require('../models/Area.model')
const ActionModel = require('../models/Action.model')
const ReactionModel = require('../models/Reaction.model')

/**
 * Connect a received action to the reactions
 * 
 * @param {number} idService The id of the action service
 * @param {number} idAction The id of the action
 * @param {json} resultJson The results of the action inside a Json
 */
exports.connectArea = async (idService, idAction, resultJson) => {

}